# ============================================================
# KI-Telefonassistent - Konfiguration
# Kopiere diese Datei zu .env und passe die Werte an
# ============================================================

# --- SIP-Trunk Einstellungen ---
SIP_PROVIDER=sipgate          # sipgate, easybell, oder custom
SIP_USERNAME=dein_sip_user
SIP_PASSWORD=dein_sip_passwort
SIP_HOST=sipconnect.sipgate.de
SIP_PORT=5060

# --- KI-Provider (wähle EINEN) ---
# Verfügbar: groq (kostenlos!), openai, gemini, anthropic, ollama
LLM_PROVIDER=groq

# --- Groq (EMPFOHLEN - kostenlos & schnell) ---
# Registrieren: https://console.groq.com (kostenlos)
GROQ_API_KEY=dein_groq_api_key
GROQ_MODEL=llama-3.1-8b-instant

# --- OpenAI (optional - ~0.002€ pro Anruf) ---
# OPENAI_API_KEY=dein_openai_key
# OPENAI_MODEL=gpt-4o-mini

# --- Google Gemini (optional - kostenloser Tier) ---
# Registrieren: https://aistudio.google.com/app/apikey
# GEMINI_API_KEY=dein_gemini_key
# GEMINI_MODEL=gemini-1.5-flash

# --- Anthropic Claude (optional - beste Qualität) ---
# ANTHROPIC_API_KEY=dein_anthropic_key
# ANTHROPIC_MODEL=claude-haiku-4-20250414

# --- Ollama (optional - nur für lokale Installation) ---
# OLLAMA_HOST=http://localhost:11434
# OLLAMA_MODEL=llama3.1:8b

# --- Piper TTS ---
PIPER_PATH=/opt/piper/piper
PIPER_VOICE=/opt/piper/voices/de_DE-thorsten-high.onnx

# --- Whisper STT ---
WHISPER_MODEL=small            # tiny, base, small (VPS), medium/large (lokal)
WHISPER_LANGUAGE=de
WHISPER_DEVICE=cpu             # cpu oder cuda (falls GPU vorhanden)

# --- Allgemeine Einstellungen ---
LOG_LEVEL=INFO
RECORDINGS_DIR=/opt/ki-telefonassistent/recordings
AUDIO_DIR=/opt/ki-telefonassistent/audio
MAX_CALL_DURATION=300          # Maximale Anrufdauer in Sekunden (5 Min)
SILENCE_TIMEOUT=5              # Sekunden Stille bevor Antwort generiert wird
GREETING_DELAY=1               # Sekunden warten vor Begrüßung

# --- Web-Dashboard ---
WEB_HOST=0.0.0.0
WEB_PORT=5000
# Wird automatisch generiert wenn nicht gesetzt:
# WEB_SECRET_KEY=
# Debug-Modus (NIEMALS in Produktion aktivieren!):
WEB_DEBUG=false
# Admin-Passwort fuer Dashboard (UNBEDINGT setzen in Produktion!):
ADMIN_PASSWORD=
# Basis-URL fuer Links in SMS (z.B. Portal-Links fuer Kunden):
# WEB_BASE_URL=https://your-server.de

# --- Aktive Branche ---
# Verfügbar: handwerk, arztpraxis, gastronomie, friseur, kosmetik, immobilien, anwalt, bestattung
ACTIVE_BUSINESS=handwerk

# --- Booking-System (Termin-/Anfragen-App) ---
# Die ID des Betriebs in der businesses-Tabelle.
# Wird automatisch gesetzt wenn ein Betrieb mit manage_business.py angelegt wird.
# Wenn gesetzt, werden Termine/Anfragen aus Anrufen automatisch in der App angelegt.
BOOKING_BUSINESS_ID=0

# --- E-Mail Benachrichtigung ---
# Der Kunde bekommt nach jedem Anruf eine Zusammenfassung per E-Mail
EMAIL_ENABLED=true
EMAIL_SMTP_HOST=smtp.gmail.com     # Oder: smtp.web.de, smtp.gmx.de, smtp.ionos.de
EMAIL_SMTP_PORT=587
EMAIL_SMTP_USER=deine_email@gmail.com
EMAIL_SMTP_PASS=dein_app_passwort  # Bei Gmail: App-Passwort verwenden!
EMAIL_FROM=KI-Telefonassistent <deine_email@gmail.com>
EMAIL_TO=kunde@firma.de            # Mehrere: kunde@firma.de,chef@firma.de

# --- Telegram Benachrichtigung (optional, kostenlos) ---
# 1. @BotFather auf Telegram anschreiben → /newbot → Token kopieren
# 2. Dem Bot eine Nachricht schicken
# 3. Chat-ID ermitteln: python3 -c "from src.notifications import TelegramNotifier; ..."
TELEGRAM_ENABLED=false
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789         # Mehrere: 123456789,987654321
